import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
   colors: {
     bg: "white",
     bodyColor: "black",
   },

  fonts: {
    heading: "Open sans",
    fontBody: "Segeo UI",
  },
});


export default theme